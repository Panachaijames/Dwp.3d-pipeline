# Prepage assets

Drop your realistic house render here as **`house.png`** (or `.jpg` — then update the
`src` in `components/showcase/ShardImage.tsx`).

- Served at `/prepage/house.png`.
- Used by the "02 · Render" section of the showcase landing (`/`): the image is
  fractured into a grid of tiles that fly in and assemble as you scroll.
- Recommended: a landscape render (~1600×1000, 16:10-ish) of the villa exterior.
- Until you add it, a labelled placeholder is shown automatically.
